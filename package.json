{
  "private": true,
  "workspaces": [
    "global-types",
    "url/types",
    "buffer/types"
  ],
  "scripts": {
    "build:fetch": "node scripts/build-fetch.mjs",
    "build:fetch-event-source": "node scripts/build-fetch-event-source.mjs",
    "test:types": "tsc -p types/tsconfig.json"
  },
  "devDependencies": {
    "@microsoft/fetch-event-source": "2.0.1",
    "bare-fetch": "3.2.0",
    "bare-form-data": "1.2.2",
    "esbuild": "^0.25.0"
  }
}
