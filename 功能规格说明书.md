# WHATWG Streams —— 功能规格说明书与 Goja 集成基线

> **版本**: 3.1（增加文本转换流与独立 Node classic streams facade 计划）
> **用途**: 作为 Streams 模块的外部行为、集成边界和测试依据。
> **实现说明**: 本文档描述**外部可观察行为**和 Goja 集成契约；内部状态机由 vendor 的成熟 polyfill 提供，不在 Go 层重复实现。
> 1. 公开的 **WHATWG Streams 标准**（https://streams.spec.whatwg.org/，可自由实现）；
> 2. 本项目**公开接口定义**（模块名、导出名、Go 集成 helper）。
>
> vendor 来源和许可证记录在 `streams/internal/polyfill/README.md` 与同目录 `LICENSE`。
> 行为校验以 WHATWG Streams 和 polyfill 自带 WPT 目标为准。

---

## 0. 需求调整摘要（v3.0）

1. **引擎更换**：基于**原生 goja**（`github.com/dop251/goja`）+ **goja_nodejs**（`github.com/dop251/goja_nodejs`，含 `require` 与 `eventloop`），**不再依赖 sobek / k6 模块体系**。
2. **交付形态**：实现为**一个 goja_nodejs 原生模块**（通过 `require.RegisterNativeModule` 注册，JS 侧 `require("<模块名>")` 使用）。
3. **目标**：尽可能暴露成熟 polyfill 的完整 Web Streams API；Go 层只负责模块装配、runtime 隔离和文件系统等宿主集成。
4. **重大简化**：goja 具备**规范正确的 Promise 微任务队列**（异步 job queue），故 sobek 版中为补偿"同步结算"而引入的事务/延迟结算机制**不再需要**；同时 `new` 语义、属性描述符等均有原生支持，**不再需要 JS 包装构造器 hack**。
5. **固定实现基线**：vendor `web-streams-polyfill@4.3.0` 的 `dist/ponyfill.js` ES2015 构建；ES5 构建已验证可被 Goja 解析，但当前不作为默认嵌入产物。

---

## 1. 项目概述

### 1.1 技术栈
| 项 | 值 |
|---|---|
| 语言 | Go |
| JS 引擎 | `github.com/dop251/goja`（ES 5.1+，规范微任务队列） |
| 模块系统 | `github.com/dop251/goja_nodejs/require` |
| 事件循环 | `github.com/dop251/goja_nodejs/eventloop` |
| 交付形态 | 单一原生模块，JS 侧 `require("...")` 加载 |
| 实现范围 | vendor polyfill 的 Web Streams 构造器、文本转换流，以及可独立启用的 Node classic streams facade（streamx 引擎 + canonical events） |
| Goja 集成 | `streams`、`stream/web`、`node:stream/web`、可选 `stream`/`node:stream`；以及基础模块 `events`、`path`、`string_decoder`、`timers`(+`timers/promises`)；Go 侧创建、识别、消费 helper |
| 明确限制 | Goja 当前脚本语法不支持 `async`/`await` 语法；可使用 Promise 链和 async iterator 对象 API |

### 1.2 模块注册契约
- 用 `require.RegisterNativeModule("<name>", loader)`（或 `Registry.RegisterNativeModule`）注册。
- `loader func(*goja.Runtime, *goja.Object)`：第二个参数为模块对象，向其中填充 `exports`。
- `exports` 暴露 vendor polyfill 的 13 个名字（见 §3.0），并补充 `TextEncoderStream`、`TextDecoderStream`；同一 runtime 内保持构造器 identity。
- 默认注册 `streams`、`stream/web`，并由 `RegisterCoreModule` 自动提供对应 `node:` 别名。
- 基础模块默认注册：`events`/`node:events`、`path`/`node:path`、`string_decoder`/`node:string_decoder`、`timers`/`node:timers`、`timers/promises`/`node:timers/promises`。`timers` 依赖 eventloop 安装的全局 timer，无 eventloop 时调用即抛明确错误。

### 1.3 运行时装配契约
- 宿主用 `require.NewRegistry(...)` + `registry.Enable(runtime)` 装配 `require()`；
- 宿主用 `eventloop.NewEventLoop(eventloop.WithRegistry(reg))` 运行脚本；
- **所有流操作必须在事件循环上下文中执行**（`loop.Run` / `RunOnLoop`），保证 Promise job 被消费（§5.3）。

---

## 2. 能力矩阵

| # | 能力 | 本实现 |
|---|---|---|
| 1 | `ReadableStream` 默认流、byte stream、`from`、`values`、async iterator | ✅ vendor polyfill |
| 2 | Default reader、BYOB reader、BYOB request | ✅ vendor polyfill |
| 3 | `WritableStream`、default writer、背压、controller | ✅ vendor polyfill |
| 4 | `TransformStream` 与 Transform controller | ✅ vendor polyfill |
| 5 | `tee()`、`pipeTo()`、`pipeThrough()` | ✅ vendor polyfill |
| 6 | `ByteLengthQueuingStrategy`、`CountQueuingStrategy` | ✅ vendor polyfill |
| 7 | Promise 微任务顺序 | ✅ 由 polyfill + Goja job queue 提供 |
| 8 | Web IDL/品牌检查/原型描述符 | ✅ 由 ES2015 构建提供 |
| 9 | `WritableStreamDefaultController.signal` | 可选：runtime 提供 `AbortController` 时启用 |
| 10 | Go 侧 callback source/sink、reader 消费 | ✅ `streams/integration.go` |
| 11 | `TextEncoderStream` / `TextDecoderStream` | ✅ 基于 canonical `TransformStream` 和 UTF-8 codec |
| 12 | Node classic `Readable` / `Writable` / `Duplex` / `Transform` | 独立 facade（streamx 引擎），允许单独启用 |

> **明确边界**：Streams 模块本身尽可能完整；FS 模块仍只依赖默认 reader/writer，不承诺 Node.js `stream` 的 EventEmitter、`data`、`drain`、`pipe()` 语义。

---

## 3. 对外公开的 JS API 契约

### 3.0 模块导出清单
`ByteLengthQueuingStrategy`、`CountQueuingStrategy`、`ReadableByteStreamController`、`ReadableStream`、`ReadableStreamBYOBReader`、`ReadableStreamBYOBRequest`、`ReadableStreamDefaultController`、`ReadableStreamDefaultReader`、`TransformStream`、`TransformStreamDefaultController`、`WritableStream`、`WritableStreamDefaultController`、`WritableStreamDefaultWriter`、`TextEncoderStream`、`TextDecoderStream`。

### 3.0.1 文本转换流

`TextEncoderStream` 和 `TextDecoderStream` 是独立于宿主全局 `TextEncoder` / `TextDecoder` 的 canonical constructors：

- `TextEncoderStream`：输入字符串，输出 UTF-8 `Uint8Array` chunk；跨 chunk 的 surrogate pair 必须在转换器内部正确处理。
- `TextDecoderStream`：输入 `ArrayBuffer`、TypedArray 或 DataView，输出字符串；默认使用 UTF-8，跨 chunk 的不完整多字节序列必须保留到下一个 chunk。
- 两者都暴露 `.readable` 和 `.writable`，底层由 canonical `TransformStream` 提供背压、error、close 和 abort 行为。
- `TextDecoderStream` 暂不承诺非 UTF-8 编码；后续编码扩展不得改变默认 UTF-8 行为。

### 3.0.2 Node classic streams facade

Node classic streams 放在独立的 `streams/node` 包，不改变 `stream/web` 的构造器 identity：

- 模块名：`stream`、`node:stream`。
- 构造器：`Readable`、`Writable`、`Duplex`、`Transform`、`PassThrough`。
- 静态/辅助 API：`Readable.from`、`pipeline`、`finished`、`addAbortSignal`、`isReadable`、`isWritable`、`isEnded`、`isFinished`、`isDisturbed`、`getStreamError`、`duplexPair`。
- 事件模型：`EventEmitter` 风格的 `data`、`readable`、`end`、`finish`、`close`、`error`、`drain`、`open`。
- 适配器：`Readable.fromWeb/toWeb`、`Writable.fromWeb/toWeb`、`Duplex.fromWeb/toWeb`，基于 canonical `web-streams-polyfill`（`stream/web`）实现；两套对象不通过 `instanceof` 互相伪装。
- 底层引擎：bundled `streamx@2.28.0`（esbuild，见 `streams/internal/streamx`），事件基座复用 canonical `events` 模块（bare-events 改编），构造器身份与 `require("events")` 一致。
- 已知差异（vs readable-stream/Node）：字符串 chunk 在写/推时被转换为 `Uint8Array`（需 `Buffer.from(chunk).toString(encoding)` 解码）；`read(0)` 不支持；默认 destroy 错误为 `STREAM_DESTROYED`；状态存于 `_duplexState` 位掩码。

### 3.1 `ReadableStream`

**构造**：`new ReadableStream(underlyingSource?, strategy?)`

构造行为契约：
1. 先校验 `strategy`（见 §4.3），再处理 `underlyingSource`；
2. 转换 `underlyingSource`（IDL 字典，见 §4.4）；
3. 若 `underlyingSource.type` 为 `"bytes"` → 创建 byte stream，并允许 BYOB reader；
4. 创建内部队列、读取器槽位、状态机，并调用 `underlyingSource.start`（若提供）。

**实例成员**：

| 成员 | 类型 | 行为契约 |
|---|---|---|
| `locked` | getter → boolean | 是否有锁（任何 reader 获得过且未释放/未取消） |
| `cancel(reason?)` | 方法 → Promise | 若已锁定则 reject TypeError；否则取消流，reason 传给 `underlyingSource.cancel`，结果 Promise 兑现（undefined） |
| `getReader(options?)` | 方法 → 对象 | 无 options → 返回 `ReadableStreamDefaultReader`；byte stream + `{mode:"byob"}` → 返回 `ReadableStreamBYOBReader` |
| `tee()` | 方法 | 返回两个分支流，遵循 polyfill/WHATWG 语义 |

### 3.2 `ReadableStreamDefaultReader`

**构造**：`new ReadableStreamDefaultReader(stream)`，`stream` 必须是 `ReadableStream` 实例（否则抛 TypeError）。

**实例成员**：

| 成员 | 类型 | 行为契约 |
|---|---|---|
| `closed` | getter → Promise | 流关闭 → 兑现；流出错 → reject（拒绝原因 = 流错误）；reader 释放锁 → reject TypeError |
| `read()` | 方法 → Promise | 兑现对象 `{ value, done }`；有数据 → `{value: chunk, done: false}`；流关闭 → `{value: undefined, done: true}`；流出错 → reject |
| `cancel(reason?)` | 方法 → Promise | 委托到流取消 |
| `releaseLock()` | 方法 → void | 释放锁；若有未决 read 请求 → 全部 reject TypeError；`closed` 承诺被 reject |

**行为要点**：
- 一个流同一时刻只能有一个 reader（`getReader()` 或构造 reader 时若 `locked` → 抛 TypeError）。
- 读取遵循 FIFO 队列顺序。

### 3.3 `WritableStream`

**构造**：`new WritableStream(underlyingSink?, strategy?)`

构造行为契约：
1. 转换 `underlyingSink`；
2. 若 `underlyingSink.type` 存在（含 `null`）→ 抛 `RangeError`（只支持默认类型）；
3. 创建控制器/队列/状态机，调用 `underlyingSink.start`（若提供）。

**实例成员**：

| 成员 | 类型 | 行为契约 |
|---|---|---|
| `locked` | getter → boolean | 是否有 writer |
| `abort(reason?)` | 方法 → Promise | 锁定则 reject TypeError；否则中止流，调 `underlyingSink.abort`，相关 pending 操作 reject |
| `close()` | 方法 → Promise | 锁定则 reject TypeError；已在关闭中则 reject；否则排队关闭 |
| `getWriter()` | 方法 → 对象 | 返回 `WritableStreamDefaultWriter`（自动获取写锁） |

### 3.4 `WritableStreamDefaultWriter`

**构造**：`new WritableStreamDefaultWriter(stream)`，`stream` 必须是 `WritableStream` 实例（品牌检查，否则 TypeError）；若流已锁定抛 TypeError。

**实例成员**：

| 成员 | 类型 | 行为契约 |
|---|---|---|
| `closed` | getter → Promise | 流关闭 → 兑现；流出错/中止 → reject；释放锁 → reject TypeError |
| `desiredSize` | getter → number \| null | 流关闭/errored → null；否则返回背压期望值（HWM − 队列总尺寸） |
| `ready` | getter → Promise | desiredSize > 0 时已兑现；需要背压时 pending，解除背压时兑现；出错/释放锁 → reject |
| `write(chunk)` | 方法 → Promise | 写入；内部按队列排队，逐块调用 `underlyingSink.write`；出错 → reject |
| `close()` | 方法 → Promise | 排入关闭；等待队列清空后调 `underlyingSink.close`；`ready` 不受关闭影响 |
| `abort(reason?)` | 方法 → Promise | 中止写入 |
| `releaseLock()` | 方法 → void | 释放锁；使 `ready`/`closed` reject TypeError |

### 3.5 `CountQueuingStrategy`

**构造**：`new CountQueuingStrategy({ highWaterMark })`

**实例成员**：
- `highWaterMark`：只读，构造时传入的数字。
- `size()`：只读，恒返回 `1`（按"块数"计背压）。

### 3.6 控制器对象（不导出全局，仅作为回调参数传入）

| 对象 | 回调传入位置 | 成员 | 契约 |
|---|---|---|---|
| `ReadableStreamDefaultController` | `underlyingSource.start/controller`、`pull/controller` | `desiredSize`（getter，number 或 `null`）、`close()`、`enqueue(chunk)`、`error(e)`、`constructor`（只读，非可构造） | `close()`/`enqueue()` 在不可用时抛 TypeError（已 close/已 errored/流已关闭）；`desiredSize` 在流 errored 或 controller 未启动时为 `null` |
| `WritableStreamDefaultController` | `underlyingSink.start/controller`、`write(chunk, controller)` | `error(e)`、`signal`（若宿主提供 `AbortController`）、`constructor`（只读，非可构造） | `error(e)` 仅当流仍可写时生效 |

**注意**：`start` 与后续 `write`/`pull` 收到的**必须是同一个控制器对象实例**。

---

## 4. 行为契约细节

### 4.1 状态机

**ReadableStream 状态**：`readable` → `closed` | `errored`。
- 所有状态转换遵循规范 §4.2；
- `close()` 后不再接受 `enqueue`；`error(e)` 使流进入 `errored` 并 reject 所有 pending 请求。

**WritableStream 状态**：`writable` → `erroring` → `errored`；`writable` → `closed`。
- `abort()` / 写入出错 → `erroring`，在 in-flight 操作结束后进入 `errored`；
- `close()` 排队 → 队列清空 → 调 `underlyingSink.close` → `closed`；
- 背压：`backpressure = queueTotalSize > HWM`；`ready` 承诺随背压状态切换。

### 4.2 带尺寸队列（Queue with sizes）
- 队列元素为 `{value, size}` 对，维护**尺寸总和**（total size）。
- 入队校验：size 必须为非负有限数（NaN / 负数 / +∞ → 抛 RangeError）。
- `size` 计算：当 `strategy.size` 未提供时按 `1` 计；提供时由 `strategy.size(chunk)` 调用得出，调用抛错则按规范失败路径处理（写入拒绝、关闭仍被 reject 等）。
- `enqueue`/`dequeue`/`peek`/`reset` 语义对齐规范 §4.1 的 Queue operations。
- 浮点累加需正确（总尺寸为各 size 之和，出队时扣除；测试含 `floating-point-total-queue-size`）。

### 4.3 `strategy` 解析契约
- 同时支持 `{ highWaterMark }` 与 `{ size }` 两个成员。
- `highWaterMark`：缺省用默认值（Readable: 1，Writable: 1）；必须为非负有限数，否则构造抛 RangeError/TypeError。
- `size`：必须为函数，否则抛 TypeError。
- **构造顺序**：先做 strategy 校验（WPT 构造顺序测试），再转换 underlying source/sink。

### 4.4 `underlyingSource` / `underlyingSink` 字典转换
- 按 IDL 字典语义从 JS 对象读取成员：`start`、`pull`、`cancel`（source）；`start`、`write`、`close`、`abort`（sink）。
- 成员**未设置**或值为 `undefined` → 视为无该回调，不得调用。
- 回调返回值/异常契约：
  - `start` 同步返回，返回值不参与 Promise 链；抛错 → 流进入 errored。
  - `pull` 返回 Promise，reject → 流 errored。
  - `cancel(reason)` 返回 Promise，结果作为 `cancel()` 返回的 Promise 结果。
  - `write(chunk, controller)` 返回 Promise，reject → 进入 erroring。
  - `close()` 返回 Promise，reject → 流 errored。
  - `abort(reason)` 返回 Promise。
- 转换失败（如成员类型不符）→ 构造抛错。

### 4.5 Web IDL 语义（必须遵守）
1. **构造器必须 `new` 调用**：`ReadableStream()`（无 new）、各 reader/writer、`CountQueuingStrategy` 直接调用 → 抛 TypeError。ES2015 构建的 class 语义和 `Reflect.construct(SubClass, args, NewTarget)` 派生场景由 polyfill 提供。
2. **属性描述符**：原型方法、访问器、`constructor` 和 `Symbol.toStringTag` 的可枚举性/可配置性遵循 vendor polyfill 的 ES2015 构建，并由 WPT/回归测试验证；Go 桥接不重定义这些属性。
3. **`instanceof` 正确**：同一模块实例内，所有 vended 对象/reader/writer 使用**同一 canonical prototype**（identity 一致）。
4. **品牌检查**：方法收到非本类实例的 `this` → 抛 TypeError。内部品牌字段由 polyfill 私有实现维护，Go 桥接只通过 canonical constructor 和 `instanceof` 识别。
5. **原型污染防护**：派生类构造不能污染 `Object.prototype` 或外来原型；不破坏只读属性。

### 4.6 Promise 与异步契约
- 所有异步操作返回真正的 Promise，兑现/拒绝值正确（见各 API 表）。
- 拒绝原因必须保留**用户传入的 JS 错误对象身份**（不能变成包装字符串），穿透 Go 错误管道后仍是同一 JS 对象。
- 流的 `closed` Promise 在初始化时依据流状态预设置（可兑现/可拒绝）。
- **时序要求**：兑现/拒绝的顺序必须符合规范微任务顺序（如"排队的 ready reject 必须先于排队的 ready 兑现"等）。goja 原生 job queue 保证微任务顺序（§5.3），实现不得破坏该顺序。

---

## 5. 引擎集成需求（goja + goja_nodejs）

> 本节是 v3.0 的核心变更。Streams 状态机由嵌入的 JS polyfill 执行，Goja 负责编译、运行和 Promise job；Go 代码只实现模块缓存和宿主回调桥。

### 5.1 必备引擎能力（goja）
1. **嵌入式 polyfill 执行**：通过 `go:embed` 固定 `web-streams-polyfill@4.3.0` ES2015 bundle，编译为可复用 `goja.Program`，每个 runtime 运行一次并缓存 exports。
2. **Go 侧创建/结算 Promise**：桥接 helper 使用 `runtime.NewPromise() (promise *Promise, resolve, reject func(reason interface{}) error)`；可查询 `promise.State()` 与 `promise.Result()`。
3. **微任务 job queue**：Promise reaction 以 **job** 入队（异步），在控制离开 Runtime（`leave()`）时消费，符合 ECMAScript Job 语义。**无需手工延迟结算**（§5.3 说明消费时机）。
4. **Promise rejection 追踪**：`runtime.SetPromiseRejectionTracker(tracker)` 替代手工"标记已处理"（对应 `PromiseRejectionReject` / `PromiseRejectionHandle` 两种操作）。
5. **值与函数桥接**：`runtime.ToValue`、`goja.AssertFunction`、`runtime.Try` 用于创建 underlying source/sink callback 和捕获 JS 异常。
6. **异常身份**：JS 异常为 `*goja.Exception`，用 `.Value()` 取原始抛出的值；Go callback 产生的 JS 异常必须保留该值身份。
7. **instanceof**：`runtime.InstanceOf(left, right)` 只用于本 runtime 的 canonical constructor 识别。
8. **单 goroutine 约束**：一个 `*goja.Runtime` 只能被一个 goroutine 使用；所有 Go callback/结算必须在循环线程内。

### 5.2 模块与事件循环（goja_nodejs）
1. **模块注册**：`require.RegisterNativeModule("<name>", func(runtime *goja.Runtime, module *goja.Object) { /* 填充 module.exports */ })`；`Registry` 可跨 runtime 复用，模块实例每 runtime 一次。
2. **事件循环**：`eventloop.NewEventLoop(eventloop.WithRegistry(registry))`；
   - `loop.Run(fn func(*goja.Runtime))`：执行 fn，然后**持续消费 job 直到无更多延迟任务**，返回。适合测试/单脚本。
   - `loop.RunOnLoop(fn)` / `loop.Start()`：常驻场景。
   - 流的 Go 侧结算、用户回调调用等**必须发生在循环线程内**。

### 5.3 微任务与结算契约（关键）
- goja 的 Promise reaction 是**异步 job**，按入队顺序（FIFO 微任务序）执行——这正是规范语义。
- **消费时机**：job 在控制离开 Runtime 时（`leave()`）被清空，即一次 `RunString`/`RunProgram` 或一次循环 job 结束后。因此：
  - 结算动作在事件循环内触发后，其 reaction 会按序在循环后续 job 中执行；
  - **不得**依赖"结算后同步执行"的任何假设（不存在同步反应），也**不得**为排序引入额外事务机制。
- **验证**：选定 WPT 和模块回归测试验证 `ready`/`closed`/读写请求的微任务顺序；不得用同步结算替代 Promise job。
- **注意**：Promise 反应中若再触发 Go 侧操作（如 read 请求的 chunkSteps 调用 `pull`），须保证仍在循环线程内递归安全（用 `runtime.Try` 捕获用户抛错）。

---

## 6. 测试与验收标准

### 6.1 官方 WPT 套件
使用 **web-platform-tests** 的 Streams 套件作为验收基准（checkout 固定提交 + 打补丁）：

**ReadableStream（10 个套件）**：
`bad-strategies`、`bad-underlying-sources`、`cancel`、`constructor`、`count-queuing-strategy-integration`、`default-reader`、`floating-point-total-queue-size`、`general`、`reentrant-strategies`、`templated`

**WritableStream（13 个套件）**：
`aborting`、`bad-strategies`、`bad-underlying-sinks`、`close`、`constructor`、`count-queuing-strategy`、`error`、`floating-point-total-queue-size`、`general`、`properties`、`reentrant-strategy`、`start`、`write`

### 6.2 需裁剪/跳过的测试
| 类别 | 原因 | 处理 |
|---|---|---|
| 依赖宿主未提供的 `AbortController` / `DOMException` 的测试 | 这些不是 Streams 模块自己安装的全局依赖 | 仅在对应宿主能力未启用时裁剪 |
| 依赖 ES2018 `async`/`await` 语法的脚本包装 | Goja 当前脚本解析器限制 | 改写为等价 Promise 链，不裁剪 Streams 行为 |

> **注意（与 v1/sobek 版差异）**：sobek 版因"同步反应"限制而注释的**微任务交错顺序测试**（如 `reentrant-strategy` 中递归 `writer.write` 于 `size()` 内的测试）在 goja 下应可原样通过。不得沿袭旧补丁。

### 6.3 回归测试重点（必须覆盖）
1. 控制器 `constructor` 属性在两个流类型上抛 TypeError（不可构造）。
2. `underlyingSink` 回调未定义/为 `undefined` 时被忽略；`type: null` 抛 RangeError。
3. `start` 与 `write` 收到**同一个**控制器对象实例。
4. `size()` 抛错时：该次写入拒绝，**且 close 仍被拒绝**。
5. 排队的 `ready` reject（releaseLock）必须先于排队的 `ready` 兑现处理（**微任务顺序**）。
6. `instanceof`、描述符标志、new 要求、外来 receiver 拒绝、派生构造不污染外来原型/`Object.prototype`。
7. 模块装配：`require("streams")`、`require("stream/web")`、`require("node:stream/web")` 可用，三者共享构造器 identity，导出 15 个 Web Streams/文本流名字。
8. 事件循环集成：脚本以 `loop.Run` 运行，异步流程完成后无遗留 job、无死锁。
9. 文本转换：`TextEncoderStream` 和 `TextDecoderStream` 在跨 chunk UTF-8 输入下保持正确的编码边界。
10. Node classic facade：若启用，`stream` 与 `node:stream` 共享构造器 identity，事件和背压行为通过独立回归；该层失败不得影响 Web Streams。

### 6.4 验收通过标准
- 上述 WPT 套件在裁剪后全部通过（0 失败）；
- 回归测试全部通过；
- `go vet` / `go build` 无错误。

---

## 7. 已知差异与注意事项
1. `WritableStreamDefaultController.signal` 是否可用取决于宿主是否在安装 Streams 前提供 `AbortController`；Streams 模块不会偷偷安装 AbortController。
2. Goja 当前脚本解析器不接受 `async`/`await` 语法，但 async iterator 对象和 `ReadableStream.values()` 仍可通过 Promise API 使用。
3. 微任务时序由 goja 原生保证，较 sobek 版**更接近规范**；此前 sobek 的事务补偿机制废弃。
4. 模块为 goja_nodejs 原生模块，不再依赖 k6 的 `modules.VU`/`modulestest`；测试基座改为 goja + eventloop。
5. goja 非 goroutine-safe：所有引擎调用收敛在事件循环线程。
6. goja 未定义 `Symbol.asyncIterator`；`streams` 与 `events` 模块在装配时将其设为 `Symbol.for("Symbol.asyncIterator")`（规范一致），保证 web-streams-polyfill 的 `ReadableStream.from` 与 streamx 的 async iterator 使用同一符号。
7. `queueMicrotask` 由 `eventloop.NewEventLoop` 安装（`Promise.resolve().then(fn)`，走 goja 原生微任务队列）；顶层脚本用 `const { setTimeout } = require("timers/promises")` 会与全局 `setTimeout` 冲突（goja 的 TDZ 报错），模块内的 `const` 不受影响。

---

## 8. 开发任务拆解清单（供排期）

| # | 模块 | 交付物 | 依赖 |
|---|---|---|---|
| 1 | vendor 基线 | 固定 `web-streams-polyfill@4.3.0` ES2015 bundle、MIT license、SHA-256 记录 | npm package |
| 2 | 模块装配 | embed、Goja compile/cache、`streams`/`stream/web`/`node:` alias、全量 exports | goja_nodejs |
| 3 | Go source/sink bridge | `NewReadableStream`、`NewWritableStream`、callback 返回 Promise | 2 |
| 4 | Go stream helpers | `IsReadableStream`、`IsWritableStream`、`ConsumeReadableStream`、reader release/cancel | 2, 3 |
| 5 | 文本转换流 | `TextEncoderStream`、`TextDecoderStream`、UTF-8 跨 chunk 回归 | 2, 3 |
| 6 | Node classic facade | 独立 `stream`/`node:stream` 模块、EventEmitter 事件和 Web/classic adapter | 2, 3 |
| 7 | 行为回归 | 默认流、byte/BYOB、Transform、pipe、tee、strategy、文本转换、module identity | 2, 5 |
| 8 | FS 集成准备 | FS 只依赖 bridge/helper，不复制 Streams 状态机；`writeTextFile` 使用文本转换流 | 3, 4, 5 |
| 9 | 验收 | streams 包、全仓测试、race、vet/build；Node facade 可单独跳过 | 5, 6, 8 |

---

## 9. 验收清单（Checklist）
- [x] 模块通过 `require("streams")`、`require("stream/web")`、`require("node:stream/web")` 加载，导出 13 个基础名字和 2 个文本转换流名字
- [x] 默认流、byte stream/BYOB、TransformStream、pipe、tee、strategy 行为回归通过
- [x] Go source/sink 构造、流识别、default reader 顺序消费 helper 已实现
- [x] vendor bundle、许可证和 SHA-256 已记录
- [x] `TextEncoderStream` / `TextDecoderStream` 及 UTF-8 跨 chunk 行为
- [x] 可独立启用的 `stream` / `node:stream` classic facade
- [ ] WPT：readable/writable 套件接入 Goja runner 并完成裁剪后的全量验证
- [x] FS：Afero-backed readable/writable 和 stream-backed `writeFile` 接入
- [x] `go vet` / `go build` 干净
